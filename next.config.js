/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["avatars.githubusercontent.com", "blogpfthumb-phinf.pstatic.net"],
  },
};

module.exports = nextConfig;
